with "config/lithophane_config.gpr";
project Lithophane is

   for Source_Dirs use ("src/", "config/");
   for Object_Dir use "obj/" & Lithophane_Config.Build_Profile;
   for Create_Missing_Dirs use "True";
   for Exec_Dir use "bin";
   for Main use ("lithophane_main.adb");

   package Compiler is
      for Default_Switches ("Ada") use Lithophane_Config.Ada_Compiler_Switches;
   end Compiler;

   package Binder is
      for Switches ("Ada") use ("-Es"); --  Symbolic traceback
   end Binder;

   package Builder is
      for Executable ("lithophane_main.adb") use "lithophane";
   end Builder;

   package Install is
      for Artifacts (".") use ("share");
   end Install;

end Lithophane;
